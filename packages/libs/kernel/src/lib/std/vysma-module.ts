import { VysmaModuleArgs } from '@vysma/interfaces';

/**
 * Create new module that's ready to plug and play in any Vysma application
 * @param args Arguments for a source
 * @returns VysmaModuleRegistry
 */
export const createModule = (args: VysmaModuleArgs) => {
  return {};
};
